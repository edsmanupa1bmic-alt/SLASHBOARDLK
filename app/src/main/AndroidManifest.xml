<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.WRITE_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <queries>
        <intent>
            <action android:name="android.view.InputMethod" />
        </intent>
        <intent>
            <action android:name="android.settings.INPUT_METHOD_SETTINGS" />
        </intent>
    </queries>

    <application
        android:name=".SlashboardApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slashboard">

        <!-- Main Settings and Playground Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Slashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="Settings"
            android:theme="@style/Theme.Slashboard">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SmartbarSettingsActivity"
            android:exported="false"
            android:label="Smartbar Customizer"
            android:theme="@style/Theme.Slashboard" />

        <activity
            android:name=".SmartbarCustomizerActivity"
            android:exported="false"
            android:label="Smartbar Customizer"
            android:theme="@style/Theme.Slashboard" />

        <activity
            android:name=".ThemeSelectorActivity"
            android:exported="false"
            android:label="Keyboard Themes"
            android:theme="@style/Theme.Slashboard" />

        <activity
            android:name=".SpacebarBrandingActivity"
            android:exported="false"
            android:label="Spacebar Branding"
            android:theme="@style/Theme.Slashboard" />

        <activity
            android:name=".VoicePermissionActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />
            
        <!-- Android Input Method Service (IME) -->
        <service
            android:name=".service.SinhalaIME"
            android:label="@string/ime_service_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <service
            android:name=".service.SlashboardImeService"
            android:label="@string/english_ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
